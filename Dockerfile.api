FROM python:3.12-slim

WORKDIR /app
COPY pyproject.toml ./
COPY apps/api ./apps/api
RUN pip install --no-cache-dir .
RUN mkdir -p /app/data

EXPOSE 8000
CMD ["python", "-m", "uvicorn", "mirsad_api.main:app", "--host", "0.0.0.0", "--port", "8000"]
